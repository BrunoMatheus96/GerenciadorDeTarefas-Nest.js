- Controller é a camada controladora do sistema. Vai receber as requisições e devolver as respostas HTTP.

- DTO significa Data Transfer Object. Objeto de transferência de dados.Basicamente é um modelo de como você espera aqueles dados. Se no login vc quer apenas e-mail e senha você cria um DTO com o login e a senha tipados isso ajudará a saber o que você espera no método e o que você devolve TB se assim você quiser.

- Helper é um facilitador um util, algum trecho de código que é útil para ser utilizado no seu código.

- Schema é parecido com o Entity/Modelo, se vc olhar o schema vem do MongoDB mas no SQL geralmente é chamado de entidade ou modelo.

- Modules é um módulo onde ficarão agrupados os recursos com as mesmas regras de negócio.

- Service é onde é aplicada a lógica da regra de negócio.